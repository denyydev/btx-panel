# BTX Admin Panel

Админ-панель для тестового задания с полным стеком технологий.

## Стек технологий

- **Next.js 16** (App Router)
- **TypeScript**
- **HeroUI** - UI компоненты
- **Tailwind CSS** - стилизация
- **SCSS** - глобальные стили и модули
- **Zustand** - управление состоянием
- **TanStack Query** - работа с API
- **Socket.IO** - real-time коммуникация
- **JWT** - аутентификация (токен в cookies)

## Структура проекта

```
src/
 ├─ app/                    # Next.js App Router страницы
 │   ├─ login/             # Страница логина
 │   ├─ dashboard/         # Защищённые страницы
 │   │   ├─ users/         # Управление пользователями
 │   │   ├─ admins/        # Управление администраторами
 │   │   └─ posts/         # Управление постами
 │   └─ layout.tsx         # Корневой layout
 │
 ├─ shared/                 # Общий код
 │   ├─ api/               # API сервисы
 │   ├─ hooks/             # Кастомные хуки
 │   ├─ store/             # Zustand stores
 │   ├─ ui/                # Переиспользуемые компоненты
 │   └─ styles/            # SCSS стили
 │
 ├─ providers/             # React провайдеры
 │   ├─ query-provider.tsx # TanStack Query
 │   └─ hero-provider.tsx  # HeroUI
```

## Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Запуск Next.js приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### 3. Запуск Socket.IO сервера (в отдельном терминале)

```bash
npm run socket
```

Socket сервер будет доступен по адресу: http://localhost:3001

## Использование

### Аутентификация

1. Перейдите на `/login`
2. Введите любые email и пароль (заглушка)
3. После успешного входа вы будете перенаправлены на `/dashboard`

### Защищённые маршруты

Все маршруты `/dashboard/*` защищены middleware, который проверяет наличие JWT токена в cookies.

### TanStack Query

Все запросы к API должны идти через TanStack Query. Пример использования:

```typescript
import { useUsers } from "@/shared/hooks/useUsers";

function UsersPage() {
  const { data, isLoading, error } = useUsers();
  // ...
}
```

### Socket.IO

Socket.IO подключение происходит автоматически в `dashboard/layout.tsx`. Для использования в компонентах:

```typescript
import { useSocket } from "@/shared/hooks/useSocket";

function MyComponent() {
  const socket = useSocket();
  // ...
}
```

## Архитектурные решения

1. **Разделение на слои**: код разделён на `app`, `shared`, `providers`
2. **API через TanStack Query**: все запросы к API идут через TanStack Query
3. **Zustand для состояния**: глобальное состояние через Zustand stores
4. **JWT в cookies**: токен аутентификации хранится в cookies
5. **Middleware защита**: защита маршрутов через Next.js middleware
6. **SCSS модули**: стилизация компонентов через SCSS модули

## TODO

- [ ] Подключить реальное API
- [ ] Реализовать полноценный CRUD
- [ ] Добавить валидацию форм
- [ ] Настроить обработку ошибок
- [ ] Добавить тесты

## Лицензия

MIT

# Admin Panel — Test Task (Middle Frontend Developer)

Админ-панель, разработанная в рамках тестового задания на позицию **Middle Frontend Developer**.  
Проект демонстрирует работу с Next.js App Router, HeroUI, TanStack Query, архитектурный подход,
верстку по макету Figma, взаимодействие с API и Socket.IO.

---

## 🚀 Стек технологий

- **Next.js (App Router)**
- **TypeScript**
- **HeroUI**
- **Tailwind CSS + SCSS**
- **TanStack Query**
- **Zustand**
- **Socket.IO**
- **Next.js BFF (app/api)**

---

## 📦 Возможности

### 🔐 Аутентификация

- Авторизация через тестовый API
- Получение JWT и сохранение в cookies
- Автоматический редирект на страницу логина при отсутствии токена
- Выход из системы с очисткой токена

---

### 🧭 Главная страница (Dashboard)

- Боковая навигация:
  - **Администраторы**
  - **Пользователи**
  - **Публикации**
- Виджет текущего пользователя (аватар / fallback-иконка)
- Кнопка выхода
- Навигация между сущностями без перезагрузки страницы

---

### 👤 Пользователи

- Таблица пользователей с:
  - пагинацией
  - выбором количества строк
  - сортировкой по колонкам
- CRUD-операции:
  - создание
  - редактирование
  - удаление
- Все действия выполняются через **модальные окна**
- Простая валидация полей (email, обязательные поля, дата)
- Отображение агрегированных метрик:
  - количество постов
  - количество лайков
  - количество комментариев

#### ⚙️ Метрики пользователей

Метрики реализованы через **BFF-роут** (`/api/users/metrics`):

- агрегация данных выполняется **на сервере**
- клиент не делает N+1 запросов
- ограничен параллелизм запросов к upstream API
- повышена производительность и стабильность

---

### 🛡 Администраторы

- Отдельная страница `/admins`
- Та же таблица и CRUD, что и у пользователей
- **Только пользователи с ролью admin**
- Без метрик (упрощённый вариант по ТЗ)
- Реализовано через отдельный BFF-роут `/api/users/admins`

---

### 📰 Публикации (Posts)

- Таблица постов пользователей
- Отображение:
  - автора поста
  - просмотров
  - лайков
- Кнопка перехода к комментариям конкретного поста
- Пагинация и навигация

---

### 💬 Комментарии

- Отдельная страница комментариев поста
- Получение комментариев по `postId`
- Пагинация
- Отображение автора и текста комментария

---

### 🔔 Уведомления (Socket.IO)

- Подключён локальный Socket.IO сервер
- При CRUD-операциях над пользователями и администраторами:
  - сервер отправляет событие
  - клиент отображает toast-уведомление (HeroUI)
- Демонстрация real-time взаимодействия

---

## 🧠 Архитектура проекта

- Чёткое разделение ответственности
- Все запросы — через **TanStack Query**
- API-логика вынесена в `api-services`
- Клиент не знает о сложных агрегациях данных
- BFF слой используется для:
  - агрегаций
  - оптимизации
  - адаптации API под UI

---

## 🌐 API

Используется тестовый API:

- **Base URL:** https://test-api.live-server.xyz
- **Документация:** https://test-api.live-server.xyz/docs

> ⚠️ В документации примеры ведут на dummyjson.com,  
> но в проекте используется именно `test-api.live-server.xyz`,  
> так как `dummyjson.com` недоступен из РФ без VPN.

Изменения данных на сервере **не сохраняются** — API симулирует ответы.

---

## ▶️ Запуск проекта

```bash
npm install
npm run dev
```

- Чёткое разделение ответственности
- Все запросы — через **TanStack Query**
- API-логика вынесена в `api-services`
- Клиент не знает о сложных агрегациях данных
- BFF слой используется для:
  - агрегаций
  - оптимизации
  - адаптации API под UI

---

## 🌐 API

Используется тестовый API:

- **Base URL:** https://test-api.live-server.xyz
- **Документация:** https://test-api.live-server.xyz/docs

> ⚠️ В документации примеры ведут на dummyjson.com,  
> но в проекте используется именно `test-api.live-server.xyz`,  
> так как `dummyjson.com` недоступен из РФ без VPN.

Изменения данных на сервере **не сохраняются** — API симулирует ответы.

---

## ▶️ Запуск проекта

```bash
npm install
npm run dev
Создай .env файл:

env
Копировать код
NEXT_PUBLIC_API_URL=https://test-api.live-server.xyz
📌 Примечания
Проект ориентирован на демонстрацию архитектурного мышления

Некоторые решения (BFF, агрегации) сделаны намеренно «взрослыми»

Код адаптирован под реальное масштабирование

UI сверстан по макету Figma (без pixel-perfect, но с сохранением структуры и визуальной иерархии)

📷 Скриншоты
(добавить скриншоты интерфейса: Users / Admins / Posts / Comments / Login)

🧑‍💻 Автор
Тестовое задание выполнено в рамках отклика на позицию Middle Frontend Developer.
```
